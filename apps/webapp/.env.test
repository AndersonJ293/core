# Test Environment Variables for CORE Webapp
# These are dummy values for Jest tests - NOT real secrets

POSTGRES_DB=test_core
DATABASE_URL=postgresql://test:test@localhost:5432/test_core
DIRECT_URL=postgresql://test:test@localhost:5432/test_core

# Authentication (dummy test values)
SESSION_SECRET=test-session-secret-for-jest-tests-only
ENCRYPTION_KEY=test-encryption-key-for-jest-tests-only-32-chars
MAGIC_LINK_SECRET=test-magic-link-secret-for-jest-tests

# Neo4j (dummy test values)
NEO4J_URI=bolt://localhost:7687
NEO4J_USERNAME=test
NEO4J_PASSWORD=test

# AI Models (dummy test values)
OPENAI_API_KEY=test-api-key-for-jests
OPENAI_BASE_URL=https://api.openai.com/v1
MODEL=gpt-4

# Email service (dummy test values)
EMAIL_FROM=test@example.com
SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_USER=test
SMTP_PASS=test

# Redis (dummy test values)
REDIS_URL=redis://localhost:6379

# Application settings
NODE_ENV=test
APP_ORIGIN=http://localhost:3033

# Trigger.dev (dummy test values)
TRIGGER_PROJECT_ID=test-project-id
TRIGGER_SECRET_KEY=test-secret-key
TRIGGER_API_URL=https://api.trigger.dev
QUEUE_PROVIDER=trigger